import FieldNotes from "./FieldNotes";

export const metadata = {
  title: "Field Notes — SLBH",
  description:
    "A navigable three dimensional field of sketch images floating in dark space, drag to orbit, scroll to zoom, pan to move through the field.",
};

export default function FieldNotesPage() {
  return <FieldNotes />;
}
